{
  "name": "setup-fvm",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build --target=node --outdir=dist ./src/main.ts ./src/post.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@actions/cache": "^4.1.0",
    "@actions/core": "^2.0.1",
    "@actions/exec": "^1.1.1",
    "@actions/glob": "^0.5.0"
  }
}
